# Documentation

This skill uses a preview-first workflow: read the content brief, confirm the style, build a full prompt, generate a preview image, review the preview, reconstruct an editable PPT, and validate preview–PPT consistency. Please start with `../README.md` and `../SKILL.md`.
